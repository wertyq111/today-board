# today-board lessons

| 日期 | 教训 | 后续规则 |
| --- | --- | --- |
| 2026-04-24 | 开始新任务前必须先看 `lessons.md`，再看 `project-info.json`、`task_plan.md`、`progress.md`、`findings.md` | 不要跳过接力文件直接改代码 |
| 2026-04-24 | 当前项目不是 git 仓库 | 不说 PR、合并 main、提交 commit；除非项目后续接入 git |
| 2026-04-24 | 项目定位是 local-only HarmonyOS NEXT 应用 | 不引入后端、不走远端部署，除非用户重新确认 |
| 2026-04-24 | 生产代码严禁假数据 | 天气不可用、地图 Key 未配置、接口失败都要直接暴露，不做隐藏兜底 |
| 2026-04-24 | `@kit.WeatherServiceKit` 静态导入在当前设备缺 HSP 时会导致入口崩溃 | 不再静态接入该 Kit；要接天气前先确认目标设备运行组件存在 |
| 2026-04-24 | 地图地址搜索已走高德 Web 服务 API + 本机 Key | Key 通过设置页或本机忽略配置文件保存；未配置时关闭搜索，不造地点结果 |
| 2026-04-24 | `.gitignore` 已忽略 `local.properties` 和 `project-*.json` | 敏感配置和本机项目信息不要写进源码或资源文件 |
| 2026-04-24 | 用户要求固定地图 Key 时，只能放 `entry/src/main/ets/config/MapSearchLocalConfig.ets` 这类已忽略本机文件 | 不要把真实 Key 写进文档、源码说明、资源文件或进度记录 |
| 2026-04-24 | DevEco `Run entry` 已验证可启动，入口崩溃已解除 | 后续运行问题优先用 DevEco/设备真实验证，不凭旧 FaultLog 判断 |
| 2026-04-24 | 用户指出 APP 看起来粗糙 | UI 改版先定一个明确视觉隐喻，再用真机截图验收，不做普通卡片堆叠 |
| 2026-04-24 | ArkUI 做毛玻璃不能照搬 Web `backdrop-filter` | 用渐变背景、半透明面板、高光边框、柔和阴影实现，并以真机截图修正大字号截断 |
| 2026-04-24 | 首页毛玻璃不能只把卡片刷成浅蓝白 | 必须拆成天气背景层、半透明玻璃内容层、固定底部导航层，否则视觉不像参考图 |
| 2026-04-24 | ArkUI 8 位色值按 ARGB 处理 | 写透明蓝色要用 `#AARRGGBB`，不要按 Web 的 `#RRGGBBAA` 写，避免蓝色云层变黄绿色 |
| 2026-04-24 | 终端跑 DevEco `hvigorw` 时 `DEVECO_SDK_HOME` 不能指到 `sdk/default` | 临时命令里用 `DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk`，否则会报 SDK path 或 component 错误 |
| 2026-04-27 | 用户对当前 APP 页面不满意时，不要继续边写边猜 | 先出 4 个底部 Tab 的统一设计图，确认视觉和信息职责后再落 ArkTS |
| 2026-04-27 | 页面图标不能只放大，必须和文字按固定槽位对齐 | 快捷入口、卡片图标、底部导航都直接使用同一套按钮 PNG，并给图标、标题、副文案固定高度 |
| 2026-04-27 | “我的”页不能套旧设置页样式 | 底部 Tab 对应页面必须沿用首页的天空背景、浅蓝毛玻璃卡片和按钮图标体系 |
| 2026-04-28 | 图标资源同为 `144x144` 也会视觉错位 | ArkUI 里图标必须放进固定高度 `Stack`，文字必须设置固定高度和 `lineHeight`，不要靠 `Column(space)` 自然排 |
| 2026-04-28 | 设计稿底部导航不能为了放大图标做得过高 | 底栏要保持轻量，图标放大也要控制在固定导航高度内，不抢首屏内容空间 |
| 2026-04-28 | 底部导航必须固定在屏幕底部 | 内容区单独滚动，导航层贴底并加层级，不能随内容流自然排版 |
| 2026-04-28 | 从通勤 App 改成无后端天气看门板时，先断主导航的旧语义 | 底部 Tab 固定为看板、清单、提醒、我的，地图和通勤不再作为主路径 |
| 2026-04-28 | 天气看门板主路径不能继续依赖旧 `SetupStore` | 首页、清单、提醒、我的和服务卡片统一读 `BoardStore`；旧通勤/地图文件只能作为未注册历史代码，不能被入口初始化 |
| 2026-04-28 | 终端 HDC 在 Codex 沙箱里可能报 `Connect server failed`，但 DevEco 设备链路仍然可用 | 遇到这种情况先看 DevEco Run 面板，再用沙箱外 HDC 抓图/验收，不要误判成设备断开 |
| 2026-04-28 | 纯脚本硬画整套 UI 图标会显得粗糙，不能冒充精制设计 | 图标重设计要先用真实视觉模型或 PS 产出高质量母版，再做切图和 ArkTS 接入；脚本只适合批量裁切、压缩和资源同步 |
| 2026-04-28 | 用户要求还原图标时，不能把中间失败版本当原始版本 | 先找真正更早的 `tb_icon_*` 资源来源，再映射到新增资源名；还原后必须构建、安装、截图核对 |
| 2026-04-28 | ArkUI 自定义组件属性不要命名为 `size` | `size` 会和基础组件属性冲突；图标组件统一用 `iconSize` |
| 2026-04-28 | 系统符号名不能按直觉猜 | 使用 `SymbolGlyph` 前先查 SDK 里的 `sys.symbol.*`；头像符号本轮实际可用的是 `person_crop_circle_fill` |
| 2026-04-29 | 用户要求 Apple 无蓝色版时，资源图里的蓝色高光也算残留 | 除了 ArkUI 按钮、文字、图标和底栏，还要检查主视觉 PNG 等位图资源 |
| 2026-04-29 | 设备缺 WeatherServiceKit HSP 时，动态 `import('./WeatherKitProvider')` 也可能触发 fatal | 先用 `canIUse('SystemCapability.Weather.Core')` 判断能力，缺能力时直接写真实不可用状态，不能加载含 `@kit.WeatherServiceKit` 顶层导入的文件 |
| 2026-04-29 | 移除“目标时间”不能只删配置字段 | 还要扫描服务状态文案、持久化 summary 和截图里的旧表达，避免“默认上班时间”这类语义残留 |
| 2026-04-29 | DevEco 当前打开 `.deveco-bootstrap/TodayBoardBootstrap` 时，根目录改动不会自动跑到设备上 | 真机验收前先确认 DevEco 打开的项目路径；如果是 bootstrap，就同步 `entry/src/main/ets` 后再构建安装 |
| 2026-04-29 | ArkUI 天气动效不能直接照搬 Web 8 位色值和 Builder 传参习惯 | 透明色统一按 `#AARRGGBB` 写；动态天气文字不要作为普通 Builder 参数传入，必须在组件内部直接读状态 |
| 2026-04-29 | 首页顶部不要为视觉氛围保留大块空黑区，也不要在主视觉上压大段解释文案 | 天气看板首屏优先展示品牌行、天气画面和状态卡，说明文字放到卡片状态里 |
| 2026-04-29 | ArkUI `Stack()` 默认不会把未定位子组件贴到左上 | 首页这类绝对布局容器必须显式写 `Stack({ alignContent: Alignment.TopStart })`，否则文字会看起来跑到中间 |
| 2026-04-29 | 本地 `OPENAI_BASE_URL` 可能来自 Codex 进程环境，不一定来自项目 `.env` | 排查生图/API 配置先查当前进程环境和启动器，不要把已删除的 `.env` 当成来源 |
| 2026-04-30 | 天气 MP4 不能用程序叠加雨雪效果冒充按提示词生成 | 必须按 `weather-video-prompts.md` 和同名 PNG 逐帧生成图片；每个天气单独建文件夹，压缩帧图后再合成 MP4 |
| 2026-04-30 | 天气动图帧图不能压得过糊，也不能保留 2MB 级大图 | 后续生成帧图优先保持 1024×1536 高清尺寸，用 WebP 动态质量压缩，把单张控制在 300~500K 左右 |
| 2026-04-30 | ArkUI 沉浸式窗口里改 `expandSafeArea` 不一定改变真机顶部位置 | 页面标题高度问题用明确内容 top padding 对齐，不只依赖系统安全区 |
| 2026-05-04 | 天气 MP4 和 PNG 主视觉比例不一致时，`Video.objectFit(ImageFit.Cover)` 会造成预览图和动画切换时观感跳变 | 首页天气视频可以用 `ImageFit.Cover`，但对应 PNG 必须裁成同名 MP4 的真实尺寸 |
| 2026-05-05 | 固定底部导航会遮挡首页第二张卡片时，不能只给内容末尾加空白 | 首页 `Scroll` 视口也要避开固定底栏；真机截图要同时检查首屏不遮挡和滚动后卡片完整露出 |
| 2026-05-05 | 用户要求按平台规则重新画应用图标时，不能只把旧包内图扁平化导出 | 先新画可复验母版，再让包内图标、启动图标和上传图从同一母版导出并校验哈希 |
